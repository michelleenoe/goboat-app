// import { ColorAssignment } from '../../../styles/types';
// import { ButtonColor } from './button';

// export const getBackgroundColor = (color: ButtonColor): ColorAssignment => {
//   switch (color) {
//     case ButtonColor.primary:
//       return ColorAssignment.buttonBackgroundPrimary;

//     case ButtonColor.secondary:
//       return ColorAssignment.buttonBackgroundSecondary;

//     case ButtonColor.light:
//       return ColorAssignment.buttonBackgroundLight;

//     case ButtonColor.dark:
//       return ColorAssignment.buttonBackgroundDark;

//     default:
//       return ColorAssignment.buttonBackgroundPrimary;
//   }
// };

// export const getBorderColor = (color: ButtonColor): ColorAssignment => {
//   switch (color) {
//     case ButtonColor.primary:
//       return ColorAssignment.buttonBorderPrimary;

//     case ButtonColor.secondary:
//       return ColorAssignment.buttonBorderSecondary;

//     case ButtonColor.light:
//       return ColorAssignment.buttonBorderLight;

//     case ButtonColor.dark:
//       return ColorAssignment.buttonBorderDark;

//     default:
//       return ColorAssignment.buttonBorderPrimary;
//   }
// };

// export const getTextColor = (color: ButtonColor): ColorAssignment => {
//   switch (color) {
//     case ButtonColor.primary:
//       return ColorAssignment.buttonTextPrimary;

//     case ButtonColor.secondary:
//       return ColorAssignment.buttonTextSecondary;

//     case ButtonColor.light:
//       return ColorAssignment.buttonTextLight;

//     case ButtonColor.dark:
//       return ColorAssignment.buttonTextDark;

//     default:
//       return ColorAssignment.buttonTextPrimary;
//   }
// };

// export const encodeColor = (color: string) => color.replace('#', '%23');
